# Frontend

This directory is reserved for a future React + TypeScript web application for TPM Operating System.

It intentionally contains no generated application code, package manifest, dependencies, build configuration, or API client. Frontend implementation belongs to a future feature sprint after the product contracts and API boundary are defined.
