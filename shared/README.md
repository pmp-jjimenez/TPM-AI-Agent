# Shared

This directory is reserved for versioned contracts shared across TPM Operating System boundaries.

Future contents may include API schemas, generated client contracts, event definitions, and shared domain-model documentation. Runtime business logic should remain owned by the backend, while presentation-specific models should remain owned by the frontend.

No schemas or executable shared models are introduced in this foundation sprint.
